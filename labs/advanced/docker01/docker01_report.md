# Отчёт по лабораторной работе Docker 01

## Рантайм: hardening контейнера и сегментация сети

Студент:
Группа:
Дата:
Окружение: вывод `docker version --format '{{.Server.Version}}'` и `docker compose version`

---

## Часть 0. Исходное состояние

- Вывод `./check.sh compose.insecure.yml`, файл `before.txt`
- Число открытых находок и код возврата
- Три находки, которые вы считаете самыми опасными, и почему

## Часть 1. Hardening рантайма

Для каждой меры: что изменено в `compose.hardened.yml`, команда проверки, вывод до и после.

### Пользователь

### privileged

### Сокет Docker

### Capabilities

### no-new-privileges

### Корень только на чтение и tmpfs

Приложите ошибку сервиса до добавления tmpfs и объясните причину `Permission denied` у tmpfs без `uid`.

### Лимиты ресурсов

### Seccomp

Контрольная сумма скачанного профиля, команда получения производного профиля, вывод заблокированного вызова.

### Секреты

Вывод `docker inspect` до и после. Значение пароля в отчёт не вставляется.

### Сервисы web и db

Что исправлено, что оставлено и почему.

### Rootless и user namespaces, дополнительно

## Часть 2. Сеть и сегментация

- Карта «до»: какие порты и на каких адресах слушает хост
- Схема сетей «после»: кто с кем в одной сети
- Проверки связности: web и db, api и db, выход наружу у каждого сервиса

## Часть 3. Находки, ложные срабатывания, итог

- Таблица разбора: находка, настоящая или ложная, обоснование, решение
- Содержимое `check.ignore`
- Вывод `./check.sh compose.hardened.yml`, файл `after.txt`, код возврата
- `diff before.txt after.txt`
- Сравнение раздела 5 Docker Bench из лабораторной 06 до и после

## Перенос на свой образ из лабораторной 05

- Вывод `check.sh` по своему compose-файлу до и после
- Какие меры не удалось применить и почему

## Выводы
