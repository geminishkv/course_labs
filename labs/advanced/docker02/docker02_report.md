# Отчёт по лабораторной работе Docker 02

## Поставка: сборка, SBOM, подпись

Студент:
Группа:
Дата:
Окружение: версии `docker buildx`, `trivy`, `cosign`, `hadolint`

Токен в отчёт вставляется только выдуманный. Закрытый ключ `cosign.key` в отчёт и репозиторий не попадает.

---

## Часть 0. Небрежная сборка

- Три утечки: команда, вывод, откуда взялась каждая
- Какую версию `Python` означает тег `python:slim` на день выполнения работы
- Вывод гейта «до», файл `gate_before.txt`, код возврата

## Часть 1. Цепочка поставки

### Контекст сборки

Содержимое `.dockerignore` и решение по `.env.example`.

### Базовый образ

Digest, первая строка `Dockerfile`, ответ про тег рядом с digest.

### Зависимости с хешами

Команда получения `requirements.txt`, число хешей.

### Секрет сборки и пользователь

Итоговый `Dockerfile`, вывод `hadolint`, три проверки отсутствия утечек, вывод сборки без секрета.

### Минимальная база

Сравнение двух баз по размеру и числу находок, выбор и обоснование.

### SBOM

Число компонентов, строка своей зависимости, результат скана по SBOM, ответы на вопросы.

### Подпись

Ссылка на образ по digest, вывод `cosign verify` своим ключом и чужим.

### Подмена тега

Три кода возврата и объяснение.

### Гейт и разбор находок

- Таблица разбора: находка, исправление, ложное срабатывание или принятый риск, обоснование
- Содержимое `gate.ignore`
- Вывод гейта «после», файл `gate_after.txt`, код возврата
- `diff gate_before.txt gate_after.txt`

## Часть 2. Сборка в CI

### Аргумент сборки в provenance

Число файлов с токеном для небрежной и исправленной сборки, ответ про доступность provenance.

### Сборка без демона

Зачем сборщику `seccomp=unconfined` и как этот риск соотносится с пробросом сокета.

### Конвейер скриптом

Вывод `ci/build.sh`, строка о пропущенной проверке и объяснение.

### Workflow

Ссылка на прогон и ответы на четыре вопроса по тексту workflow.

### Гейт обязан краснеть

Ссылка на красный прогон и шаг, на котором он упал.

## Перенос на свой образ из лабораторной 05

Что применено, вывод гейта, что применить не удалось и почему.

## Выводы
